interface:
  display_name: "AskChing"
  short_description: "Compare cited DeFi markets and discover USDC yields"
  default_prompt: "Use $askching to discover USDC lending and stablecoin LP yields with separate rankings, formulas, citations, risks, gaps, and as-of time."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "askching"
      description: "AskChing MCP server for cited multi-subgraph comparison, analysis, trends, and USDC yield discovery"
      transport: "stdio"
      command: "npx"
      args:
        - "-y"
        - "@askching/mcp-server"
      # ⚠️ ESC-VPW-002：Codex 的 dependencies.tools 若需 streamable_http 形式，
      # 請改用 transport: "streamable_http" + url（見 docs/cross-platform.md §4）。
      # 此處保留 stdio + command/args 作為 intended block，需在 Codex 實測驗證。
