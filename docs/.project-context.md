# Project Context Snapshot
Last Updated: 2026-09-13
Scope: full

<!-- AskChing_Agent — evidence-first DeFi market research agent（ETHOnline 2026, The Graph track）。
     本檔為 Edison skills 的快速查詢快取；若 > 7 天或結構性變更，請以 edison-project-context-snapshot 刷新。 -->

---

## Tech Stack

- Node >=20 — runtime 要求（3 個 packages 的 engines 統一）
- TypeScript 5.9（ESM NodeNext）— 全 monorepo 建置基底，`tsconfig.base.json` 為共用來源
- pnpm 9.15.4 workspaces — monorepo 管理；3 packages 以 `workspace:*` 互依
- Zod ^3.25.76 — 資料驗證層（`packages/shared/src/schemas.ts` / `packages/mcp-server/src/tools.ts`）
- @modelcontextprotocol/sdk ^1.17.5 — MCP server（stdio + Streamable HTTP 雙傳輸）
- Vitest 3 — 測試框架（`vitest.config.ts`；18–21 files、約 206 it blocks、3 個 evals contract tests）
- xAI Grok API — `api.x.ai/v1`、model `grok-4.6`（grok-orchestrator 的 LLM 後端）
- The Graph Gateway GraphQL — 唯一外部資料源（Messari lending subgraphs，4 live）
- Vercel Serverless — 部署平台（`api/*` routes + `vercel.json`，Framework Preset: Other）
- Monorepo 佈局 — `packages/shared`（@askching/shared v0.1.0）/ `packages/mcp-server`（@askching/mcp-server v0.1.0）/ `packages/grok-orchestrator`（@askching/grok-orchestrator v0.0.0 private）

## DB Entities (Tables)

- `N/A — no local database（外部資料源：The Graph Messari subgraphs；以下為 virtual schema 錨點，供下游 skills 當作 entity map）`
- Metric — `packages/shared/src/metrics.ts` — METRIC_REGISTRY：supply_apy / borrow_apy / tvl / utilization（legacy alias usdc_supply_apy → supply_apy + USDC hint）
- Asset — `packages/shared/src/schemas.ts` — 支援資產列舉（USDC 等）
- Citation — `packages/shared/src/schemas.ts` — 結構性不變量：subgraphId + block + timestamp + queryHash；fail-closed（<2 distinct subgraphId → throw）
- MarketObservation — `packages/shared/src/schemas.ts` — 市場觀測點；extends Citation（引用繼承）
- TrendPoint / TrendSeries — `packages/shared/src/schemas.ts` — 趨勢資料點；TrendPoint extends MarketObservationSchema
- Protocol Registry — `packages/shared/src/source-config.ts` — 13 條目、live=4（aave-v3 / compound-v3 / spark-lend / aave-v2）、全部 schemaVersion "3.1.0"（測試強制）
- DEX Yield Sources — `packages/shared/src/yield-sources.ts` — DEX_YIELD_SOURCES 註冊表（uniswap-v3 / curve 等，live 狀態）

## Migration History

- `N/A — 無本地 DB、無 ORM、無 migration 歷史；schema 演進由 Zod schemas + versioned subgraph schemaVersion 承載`

## Naming Conventions

- DB — `N/A（無 DB）；程式碼層級 IDs 採 snake_case`：工具名 compare_markets / analyze_trends / discover_yields、metric IDs supply_apy / borrow_apy / tvl / utilization
- Zod schemas — `PascalCase + Schema 後綴`：MarketObservationSchema / TrendPointSchema / TrendWindowSchema（證據：schemas.ts, tools.ts）
- Factories — `create*()` 前綴：createGraphClient / createAnalysisServer / createHttpServer（證據：graph-client.ts, tools.ts, http.ts）
- Interface fields — `camelCase`：marketId / subgraphId / queryHash / indexLastUpdatedTimestamp（證據：schemas.ts, source-config.ts）
- Vercel routes — `/api/*` 扁平結構：api/mcp.ts, api/health.ts（證據：api/ 目錄）
- Packages — `@askching/*` 前綴：@askching/shared / @askching/mcp-server / @askching/grok-orchestrator（證據：3× package.json）
- Env vars — `SCREAMING_SNAKE_CASE`：XAI_API_KEY / GRAPH_API_KEY / ASKCHING_LLM_MODEL（證據：.env.example）
- Source files — `kebab-case`：graph-client.ts / source-config.ts / yield-data-source.ts（證據：packages/shared/src）

## Route Inventory (Pages)

- api/mcp.ts — Vercel Serverless route — MCP Streamable HTTP endpoint（POST；`export default { fetch }` 形式，Vercel 特有）
- api/health.ts — Vercel Serverless route — 部署健康檢查（GET）
- public/index.html — 靜態 landing page — 產品展示頁（Vercel outputDirectory 依賴其存在）

## API Endpoint Inventory

- POST /api/mcp — `api/mcp.ts` — MCP Streamable HTTP；每請求新建 server/transport（stateless，SDK 強制）；非 POST → 405 + `Allow: POST, OPTIONS`
- GET /api/health — `api/health.ts` — 健康檢查；部署後驗證用（vercel:probe 目標）
- Auth — `N/A（公開 endpoints；MCP protocol 層無 auth；API keys 用於上游資料源非此處）`
- 備註 — SDK v1.30.0 的 WebStandardStreamableHTTPServerTransport（非 mcp-handler；後者需 SDK v2）

## Service Layer

- packages/shared/src/schemas.ts — 核心 Zod schemas — Citation / MarketObservation / TrendWindow / Point / Series / Stats / Finding / Result
- packages/shared/src/fixtures.ts — 測試與 demo fixtures — MARKET_HISTORY_FIXTURES（42 點）等
- packages/shared/src/graph-client.ts — The Graph GraphQL client — `switch(descriptor.extractor)` 分派、#post transport 封裝
- packages/shared/src/data-source.ts — 資料源抽象 — prepareLiveFanOut / getMarkets / getHistory
- packages/shared/src/analysis.ts — 分析邏輯 — computeTrendStats / analyzeTrendSeries / describeMissingMetric
- packages/shared/src/compare.ts — 協議比較邏輯 — 多協議引用對比
- packages/shared/src/metrics.ts — metric 註冊表 — METRIC_REGISTRY 為唯一真相來源
- packages/shared/src/source-config.ts — Protocol Registry — 13 條目、live=4、非 live 必須有 note（測試強制）
- packages/shared/src/trend.ts — 趨勢分析 — 時序統計與 finding 產生（v1.1）
- packages/shared/src/yield-sources.ts — DEX yield 註冊表 — DEX_YIELD_SOURCES（uniswap-v3/curve）
- packages/shared/src/yield-discovery.ts — yield 發現邏輯 — discover_yields 工具後端
- packages/shared/src/yield-client.ts — yield GraphQL client — Messari DEX schema 查詢（liquidityPools）
- packages/shared/src/yield-data-source.ts — yield 資料源 — 彙整 yield 觀測
- packages/shared/src/yield-fixtures.ts — yield fixtures — 測試資料
- packages/shared/src/index.ts — barrel export — @askching/shared 對外 API 面
- packages/mcp-server/src/tools.ts — 6 tools 定義 — 每 tool CoreSchema/InputSchema + transforms（zod → MCP schema）
- packages/mcp-server/src/register.ts — 工具註冊 — 唯一註冊源（registerTool ×6 + TOOL_NAMES）
- packages/mcp-server/src/index.ts — stdio entry — McpServer + StdioServerTransport
- packages/mcp-server/src/http.ts — HTTP entry — WebStandardStreamableHTTPServerTransport（stateless）
- packages/mcp-server/src/serve.ts — standalone HTTP server — 本地 serve（讀 PORT）
- packages/grok-orchestrator/src/client.ts — Grok API client — api.x.ai/v1 呼叫
- packages/grok-orchestrator/src/loop.ts — agent loop — max 4 turns tool-call 迴圈（LIVE_PROTOCOLS 由註冊表衍生）
- packages/grok-orchestrator/src/output.ts — 輸出格式化 — 人讀摘要產出
- packages/grok-orchestrator/src/index.ts — CLI entry — `pnpm askching -- "question"`
- skills/askching/SKILL.md — system prompt 來源 — grok-orchestrator 的 agent 定義（4 metrics/4 assets/6 protocols + legacy alias）

## Repository Layer

- `N/A — 無本地 DB repository layer；packages/shared/src/graph-client.ts 視為 data access layer（GraphQL 查詢封裝 + 引用擷取）`

## Validator Layer

- packages/shared/src/schemas.ts — 核心 Zod schemas — Citation / MarketObservation / TrendWindow / Point / Series / Stats / Finding / Result
- packages/mcp-server/src/tools.ts — per-tool schemas — *CoreSchema / *InputSchema（field-level MetricFieldSchema / AssetFieldSchema）
- packages/mcp-server/src/register.ts — 註冊時驗證 — SDK normalizeObjectSchema 對 ZodEffects 回 undefined 的已知問題（tools.ts 採 field-level 方案繞過）
- 驗證策略 — fail-closed citation enforcement — <2 distinct subgraphId → throw（structural invariant）

## Shared Components

- `N/A — 無 UI 組件；跨 package 共享由 @askching/shared barrel（index.ts）承載，非 component 形式`

## Custom Hooks

- `N/A — 無 React / 無前端框架；grok-orchestrator 為 CLI agent，非 hook-based`

## Environment Variables

- XAI_API_KEY — 必要 — xAI Grok API 金鑰（grok-orchestrator 的 LLM 呼叫）
- GRAPH_API_KEY — 必要 — The Graph Gateway API 金鑰（live subgraph 查詢、probe scripts）
- DEMO_LIVE — 選用 — demo 模式開關（0/1；demos/ 與展示路徑用）
- ASKCHING_LLM_BASE_URL — 選用 — LLM base URL（預設 https://api.x.ai/v1）
- ASKCHING_LLM_MODEL — 選用 — 模型名稱（預設 grok-4.6）
- ASKCHING_DEBUG — 選用 — debug 開關
- PORT — 選用 — standalone HTTP server port（serve.js 專用；.env.example 未列出）

## Key Config Files

- package.json（root）— scripts 定義 — gates：build / test / eval / mcp:smoke / mcp:http:smoke / vercel:probe / probe:protocols / probe:yields
- pnpm-workspace.yaml — workspace 定義 — 3 packages 聚合
- tsconfig.base.json — 共用 TS 基底 — ESM NodeNext / strict
- vitest.config.ts — 測試設定 — Vitest 3 全域設定
- vercel.json — Vercel 部署設定 — includeFiles 收窄為 packages/{shared,mcp-server}/dist/**
- .env.example — env 範本 — 6 個變數範例（PORT 未含）
- packages/shared/tsconfig.json — package 建置 — @askching/shared build
- packages/mcp-server/tsconfig.json — package 建置 — @askching/mcp-server build
- packages/grok-orchestrator/tsconfig.json — package 建置 — @askching/grok-orchestrator build

## Rubric Support Index

<!-- v3.5 Compact pointers。Cache 只支援 non-Critical anchors；Critical findings 一律以實時 scan 為準。
     Supported: document-review DR-D2/DR-D4/DR-D6、code-review CR-D2/CR-D3/CR-D4/CR-D7、QA QA-Q1/QA-Q2。
     Not supported（fresh-scan）: CR-D1/CR-D5/CR-D6、DR-D3/DR-D5、DR-D7。 -->

### document-review anchors

- DR-D2 accuracy anchors — ✓ — Tech Stack 版本/路徑、Service Layer 檔案地圖、API Endpoint 行為描述（405/stateless）
- DR-D4 consistency anchors — ✓ — Naming Conventions 全表、跨 package 依賴（workspace:*）、Protocol Registry 條目狀態
- DR-D6 industry/pattern hints — ✓ — Registry pattern（METRIC_REGISTRY / Protocol Registry）、fail-closed 設計、factory 慣例
- DR-D1 completeness — `partial` — 區段存在性可查；需求涵蓋度需 fresh-scan
- DR-D3 feasibility / DR-D5 risk coverage — `✗ fresh-scan`（codebase 可能 drift）

### code-review anchors

- CR-D2 architecture anchors — ✓ — 分層鏈 schemas→fixtures→graph-client→data-source→analysis→tools→register→index/http
- CR-D3 DB/ORM naming — `N/A（無 DB）`；以程式碼命名慣例替代（snake_case IDs / PascalCase+Schema / camelCase fields）
- CR-D4 cross-module anchors — ✓ — shared barrel 引用面、grok-orchestrator → mcp-server workspace 依賴
- CR-D7 Pattern Catalog inventory — ✓ — 現存 pattern 快照（Registry / Adapter(graph-client) / Factory / fail-closed invariant）
- CR-D1 plan compliance / CR-D5 security / CR-D6 performance — `✗ fresh-scan`

### QA planning anchors

- QA-Q1 traceability index — ✓ — tools → schemas → data-source 對應（6 tools 各自後端檔案）
- QA-Q2 risk-critical hotspot list — ✓ — citation enforcement（fail-closed）、live data path（GRAPH_API_KEY）、DEX live 狀態、Vercel 部署坑（export default { fetch } / outputDirectory）
- Live 驗證工具 — ✓ — probe:protocols（4/4）、probe:yields、vercel:probe、live-smoke

### Potential Improvements（升級項）

- `ESC-PCS-001` — DEX_YIELD_SOURCES 有 live:true 條目但缺同級自動化 live 一致性測試（對比 probe:protocols）— 建議新增 probe:yields 涵蓋 live 條目
- `ESC-PCS-002` — docs/product-overview.md §5.1 過時（列 5 tools，實際 6）— 建議同步
- `ESC-PCS-003` — uniswap-v3 使用 Messari DEX schema（liquidityPools / dexAmmProtocols）而非官方 schema — 決策點待解
- `ESC-PCS-004` — risk_scan 的 window 參數 required 但實際為 spot-only（無時序）— 建議改 optional 或補時序
- `ESC-PCS-005` — test count 206 vs 209 差異 — 以 terminal 實跑為準（Vitest 輸出）
- `ESC-PCS-006` — grok-4.6 已由 xAI 官方文件確認，但未以真實 API 命中
